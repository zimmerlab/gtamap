Uses the timing results created in the array job.

1. Call `mapper_time_to_tsv_fast.sh` on the output dir of the 0% mutation rate grid job (spearate for rna and dna)
2. This tsv is the used in `analyze_runtime.py` to plot all metrics
