I created a separate dataset of TTN using the provided readcounts.simulation file with 2% mutation rate

The log and stage file that are also an output of GTAMap serve as input for the py script:

```sh
python3 analyze_single.py TTN.log TTN.stage
```

