Also uses the results of the grid jobs but only of DNA mappers

Only ran for mutation rate 0 and 3

Create line plot with:
`python3 plot_gene_scores.py`


Separate metrics can be plotted like this:

```sh
compute_all.py pipeline_all_dna/pipeline_all_3/benchmarking_dataset_3_dna.all_results.tsv  pipeline_all_dna/pipeline_all_3/ dna
```

